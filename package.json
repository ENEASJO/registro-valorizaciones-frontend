{
  "name": "valoraciones-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && cp -r frontend/dist/* .",
    "install": "cd frontend && npm ci"
  }
}